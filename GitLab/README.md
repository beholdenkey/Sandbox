# GitLab SAML SSO with Keycloak

## Keycloak

1. Open your Keycloak realm.
1. Import `keycloak-gitlab.json`.
1. Fix URLs of the client.

## GitLab

1. Edit `gitlab.rb` and boot up your GitLab.
1. Click the `SAML` button.
